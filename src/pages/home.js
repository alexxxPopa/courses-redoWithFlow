// @flow

import React from 'react';
import { AppTemplate } from './components';

const Home = () => (
  <AppTemplate>
    <span>This is the Home</span>
  </AppTemplate>
);

export default Home;